function getSubstring (text, startIndex, endIndex) {
        
    const newString = text.substr(startIndex, endIndex)
        
    console.log(newString);
}

text = ("Ivan");
startIndex = 0;
endIndex = 2;
